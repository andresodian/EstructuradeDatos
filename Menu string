#include <iostream>
#include <string>

using namespace std;

string quitarVocales(string texto);
string ingresarTexto();

int main() {
    int opcion;
    do {
        cout << "******MENU******" << endl;
        cout << "1.Invertir frase" << endl;
        cout << "2.Quitar vocales" << endl;
        cout << "3.Intercambiar palabras" << endl;
        cout << "4.Sacar un substring" << endl;
        cout << "5.Contar vocales" << endl;
        cout << "6.Quitar vocales" << endl;
        cout << "7.Encontrar caracteres" << endl;
        cout << "8.Longitud" << endl;
        cout << "9.Comparar" << endl;
        cout << "0.Salir" << endl;
        cout << "Elige una opcion:" << endl;
        cin >> opcion;
        cin.ignore();

        switch (opcion) {
            case 1:
                break;
            case 2: {
                string texto = ingresarTexto();
                string resultado = quitarVocales(texto);
                cout << "Texto sin vocales: " << resultado << endl;
                break;
            }
            case 3:
                break;
            case 4:
                break;
            case 5:
                break;
            case 6:
                break;
            case 7:
                break;
            case 8:
                break;
            case 9:
                break;
            case 0:
                break;
            default:
                cout << "Opcion no valida" << endl;
        }
    } while (opcion != 0);

    return 0;
}

string quitarVocales(string texto) {
    string aux = "";
    for (int i = 0; i < texto.length(); i++) {
        if (texto[i] != 'a' && texto[i] != 'e' && texto[i] != 'i' && texto[i] != 'o' && texto[i] != 'u' &&
            texto[i] != 'A' && texto[i] != 'E' && texto[i] != 'I' && texto[i] != 'O' && texto[i] != 'U') {
            aux += texto[i];
        }
    }
    return aux;
}

string ingresarTexto() {
    string texto;
    cout << "Ingresa la frase que desees procesar: ";
    getline(cin, texto);
    return texto;
}
